export const COUNTRY_CODE_PATTERN = /^\+[1-9]\d{0,2}$/;
export const LOCAL_PHONE_PATTERN = /^\d{10}$/;

export const COUNTRY_CODE_MESSAGE = 'Country code must start with + and contain 1 to 3 digits';
export const LOCAL_PHONE_MESSAGE = 'Phone number must contain exactly 10 digits';
