export const GST_NUMBER_PATTERN = /^[0-9]{2}[A-Z]{5}[0-9]{4}[A-Z][1-9A-Z]Z[0-9A-Z]$/;
export const GST_NUMBER_MESSAGE = 'GST number must be a valid 15-character GSTIN';

export const INDIAN_STATE_CODE_PATTERN = /^\d{2}$/;
export const INDIAN_STATE_CODE_MESSAGE = 'State code must contain exactly 2 digits';
